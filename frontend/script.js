// Credit Scoring Frontend - JavaScript
document.getElementById('creditForm').addEventListener('submit',async function(e){e.preventDefault();const formData=new FormData(e.target);const data=Object.fromEntries(formData.entries());const submitBtn=e.target.querySelector('button[type="submit"]');const originalText=submitBtn.innerHTML;submitBtn.innerHTML='⏳ Processing...';submitBtn.disabled=true;setTimeout(()=>{const prediction=mockPredict(data);displayResults(prediction);submitBtn.innerHTML=originalText;submitBtn.disabled=false;document.getElementById('results').scrollIntoView({behavior:'smooth'})},1500)});
function mockPredict(data){let score=50;if(data.age>=25&&data.age<=55)score+=10;if(data.job>=2)score+=15;if(data.housing==1)score+=10;if(data.savings>=2)score+=12;if(data.credit_history>=2)score+=18;if(data.checking>=1)score+=10;if(data.employment>=2)score+=15;if(data.credit_amount<5000)score+=5;if(data.duration<=24)score+=5;const probability=Math.min(Math.max(score,30),95);const isGood=probability>=60;return{risk:isGood?'Good':'Bad',probability:probability,riskLevel:isGood?'Low':'High',recommendation:isGood?'Approve':'Review Required',confidence:probability}}
function displayResults(prediction){const resultsSection=document.getElementById('results');const resultIcon=document.getElementById('resultIcon');const resultTitle=document.getElementById('resultTitle');const resultDesc=document.getElementById('resultDesc');const riskLevel=document.getElementById('riskLevel');const confidence=document.getElementById('confidence');const recommendation=document.getElementById('recommendation');const progressFill=document.getElementById('progressFill');const probPercentage=document.getElementById('probPercentage');if(prediction.risk==='Good'){resultIcon.className='result-icon success';resultIcon.textContent='✓';resultTitle.textContent='Credit Risk: Good ✅';resultDesc.textContent='This applicant shows positive credit indicators and is likely to repay.'}else{resultIcon.className='result-icon danger';resultIcon.textContent='✗';resultTitle.textContent='Credit Risk: Bad ❌';resultDesc.textContent='This applicant shows concerning credit indicators. Further review recommended.'}riskLevel.textContent=prediction.riskLevel;riskLevel.style.color=prediction.risk==='Good'?'#10b981':'#ef4444';confidence.textContent=Math.round(prediction.confidence)+'%';recommendation.textContent=prediction.recommendation;recommendation.style.color=prediction.risk==='Good'?'#10b981':'#f59e0b';setTimeout(()=>{progressFill.style.width=prediction.probability+'%';probPercentage.textContent=Math.round(prediction.probability)+'%'},100);resultsSection.style.display='block'}
function resetAssessment(){document.getElementById('results').style.display='none';document.getElementById('creditForm').reset();document.getElementById('progressFill').style.width='0%';document.getElementById('assessment-form').scrollIntoView({behavior:'smooth'})}
document.querySelectorAll('a[href^="#"]').forEach(anchor=>{anchor.addEventListener('click',function(e){e.preventDefault();const target=document.querySelector(this.getAttribute('href'));if(target){target.scrollIntoView({behavior:'smooth',block:'start'})}})});
const inputs=document.querySelectorAll('input,select');inputs.forEach(input=>{input.addEventListener('invalid',function(e){e.preventDefault();this.classList.add('error')});input.addEventListener('input',function(){this.classList.remove('error')})});
console.log('%c💳 Credit Scoring Model Frontend','color:#6366f1;font-size:20px;font-weight:bold');console.log('%cBuilt for Code Alpha Internship','color:#8b5cf6;font-size:14px');console.log('%cPowered by Machine Learning 🤖','color:#10b981;font-size:14px');
